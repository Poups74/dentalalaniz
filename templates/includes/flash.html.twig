{% for type, message_list in app.flashes %}

        {% for message in message_list %}

        <div class="{{ type }}_message">
            {{ message }}
        </div>
            
        {% endfor %}


    
 {% endfor %}